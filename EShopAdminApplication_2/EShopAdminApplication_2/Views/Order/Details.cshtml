@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


@model EShopAdminApplication_2.Models.Order

<div class="container">
    <div>
        <a class="btn btn=success">Order for @Model.User.UserName</a>
    </div>

    @for(int i=0;i<Model.productInOrders.Count(); i++)
    {
        var item = Model.productInOrders.ElementAt(i).Product;
        if(i % 3 == 0)
        {
            @:<div class="row">
        }
        <div class="col-3 m-4">

            <div class="card" style="width: 18rem; height: 35rem">
                <img class="card-img-top" src="@item.ProductImage" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">@item.ProductName</h5>
                    <p class="card-text">@item.ProductDescription</p>
                    <h6>Price: @item.Price</h6>
                </div>

            </div>
        </div>

        if(i % 3 == 2)
        {
            @:</div>
        }
    }
</div>